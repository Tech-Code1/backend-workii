export enum EStatus {
  Pending = 'Pendiente',
  Accepted = 'Aceptado',
  Rejected = 'Rechazado',
}

/* export interface IResponseApplicationUser {
  id: string;
  user: IUser;
  isApplyWorkii: boolean;
}

export interface IUser {
  id: string;
}
 */